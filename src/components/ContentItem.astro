---
interface Props {
    title?: string;
    description?: string;
    url?: string;
    targetType?: string;
    date?: string;
    isDisplayDate?: boolean;

}
const { title, description, url, targetType="_self", date, isDisplayDate=true } = Astro.props;
---

<div class="flex flex-row flex-1 gap-8">
    <div class="flex flex-1 flex-col">
        <div>
            <a href={url} target={targetType} class="typo-link text-light-body dark:text-dark-body">{title}</a>
        </div>
        {
            description && (
                <p class="text-ssubtle text-light-subtle dark:text-dark-subtle">
                    {description}
                </p>
            )
        }
    </div>
    <div class="flex text-ssubtle text-light-subtle dark:text-dark-subtle">
        {date && isDisplayDate ? date.split(", ")[0] : ''}
    </div>
</div>

